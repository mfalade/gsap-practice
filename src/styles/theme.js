const colorMap = {
  white: '#FFF',
  red: '#942736',
};

const fontSizeMap = {
  sm: '0.625em',
  md: '0.78125em',
  lg: '1.5625em',
};

const transitionMap = {
  short: '150ms ease-in-out',
  medium: '300ms ease-in-out',
  long: '400ms ease-in-out',
};

const specsMap = {
  navbarHeight: '80px',
  footerHeight: '100px',
};

export default {
  color: colorMap,
  font: fontSizeMap,
  transition: transitionMap,
  specs: specsMap,
};
