const colorMap = {
  white: '#FFF',
  red: '#942736'
};

const fontSizeMap = {
  sm: '0.525em',
  md: '0.78125em',
  lg: '1.5625em'
};

const transitionMap = {
  short: '150ms ease-in-out',
  medium: '300ms ease-in-out',
  long: '400ms ease-in-out'
};

export default {
  color: colorMap,
  font: fontSizeMap,
  transition: transitionMap
};
